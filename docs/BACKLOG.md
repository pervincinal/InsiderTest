# Backlog (single source of work)

Priority = order within a section. Producer moves items; anyone may add to Bugs / Icebox.

## In progress (today)
(empty — next sprint picks from "Next")

## Next (ECON — Phase B/C after accounts exist)
- [ ] ECON-1 Phase B: real RevenueCat + AdMob keys (secrets `RC_*`, `ADMOB_*`), sandbox purchase test on a device, App Store Connect / Play products created with catalog ids — mobile-engineer + publisher (needs stakeholder accounts)
- [ ] QA-2 Real-device pass (Android APK, iPhone via TestFlight later) — needs stakeholder's phone
- [ ] ECON-8 `app-ads.txt` hosting needs a domain root (user-site repo or custom domain) — needs stakeholder decision

## Next
- [ ] LV-11 Weekly 3★ target: the reference bot reaches star3 on ~10/26 weeks by design (0.9 × median); watch human 3★ rate after launch, consider target = 1.1 × star3 if < 5 % of weekly players hit it — game-designer (after telemetry)
- [x] DAILY-4 (**done 2026-09-20**, 6399883, frontend + game-designer) Daily picker repeated levels with a 10-day period (FNV over `YYYY-MM-DD` collided mod 32: 15 distinct levels in 28 days, level 14 three times) — shipped as a per-cycle Fisher–Yates permutation: day `n` (days since 2026-01-01 UTC) plays position `n mod 42` of the order seeded by `FNV("cycle#" + floor(n / 42))`, so no level repeats inside a 42-day cycle (stronger than the 14-day idea; a repeat across a cycle boundary is possible and accepted, GDD §7.6); pool 9–50 (`POOL_TO` = 50); seed and twist hashes unchanged; new pins GDD §7.2 (2026-09-21 → 44 / 44606 / lean, 22 → 31 / 711749 / reinforced, 23 → 21 / 489368 / thinWalls); `weeklyFor` landed in the same commit (GDD §8, 2026-09-21 → 33 / 743047 / fastFeet). Follow-ups: QA pins the exact values in `tests/daily/challenge.test.ts`; AI Engineer re-runs `--daily 2026-09-21 --days 60 --seeds 5` and `--twist <id>` over 9–50
- [x] DAILY-3 (**closed 2026-09-20**, game-designer) Day-30 streak milestone: pays once per streak run as shipped; no repeating "every 30 days" bonus in v1 — reasoning in GDD §7.2 "Day 30", §7.6 updated; revisit only with 30 days of streak data (distinct day-60 / day-100 tiers, never a repeat)
- [ ] MM-4 (measured; device confirmation pending) PERF-3 memory on device: three viewport canvases ≈ 2 × 8–12 MB extra at DPR 3; confirm on a real phone with MM-1, fall back to two layers if a low-end device stutters — mobile-engineer
  - 2026-09-19 (mobile) **measured headless** (Playwright + CDP, level 15; docs/MOBILE.md §9): uncapped 3 layers = Pixel 5 @2.75 24.7 MB, Pixel 7 WebView @2.625 27.3 MB, iPhone-class 390×844 @3 33.9 MB (+ 9.7 MB terrain cache; a 430×932 @3 phone would be 43 MB). Added a DPR cap in `src/render/view.ts` (`DPR_CAP` native 2 / web 2.5 / max 3, `?dprcap=N` override, unit test `tests/render/view.test.ts`): native DPR 3 → 15.1 MB, web → 23.5 MB, Pixel 5/7 native → 13.1 / 15.8 MB. Text at cap 2 is one device px softer only at 3× zoom. Two-layer fallback (HUD merged into the game canvas) documented, to be used only if a low-end phone still stutters.
- [x] DOCS-2 (**GDD done 2026-09-22**; PERF-4 tower sprite cache shipped 2026-09-25, 3dc4e19) DOCS-2 (GDD half done 2026-09-22: §7.3 `daily.streakNext` / `daily.milestone` with code refs and show condition, §7.6 streak milestones marked shipped; §2.1–§2.4, §2.6, §2.7, §4 swept to rules v3, "Designer's read" appended to §2.0b with the SHIELD_MS proposal) — remaining: PERF-4 tower sprite cache (7–14 %) stays optional after PERF-3 — tech-artist only
  - 2026-09-18 (QA) **CI gates done** — `.github/workflows/tower-clash-ci.yml` runs, after the headless playtest, "Daily challenge gate" (`npm run playtest -- --daily $(date -u +%F) --days 30 --seeds 3`, 2.4 s locally) and "Twist pool gate" (all five twists at `--seeds 5`, ≈ 2.6 s each; K = 5 is the GDD §7.5 gate 4/5 — at K = 3 the 80 % rule demands 3/3 and level 18 loses seed 1 under lean, so the requested `--seeds 3` would be red). Documented in `.claude/skills/playtest/SKILL.md`. The streak-milestone part stays open (monetization + frontend).
- [ ] PUB-9 Release `tower-clash-v1.0.0`: tag from the GitHub UI on 4b7738f or later (version 1.0.0 / build 6 since ac41e5d; this environment cannot push tags; exact steps in docs/publishing/LAUNCH_CHECKLIST.md §0 EN/AZ), then enable GitHub Pages for the web build — stakeholder / producer
- [x] AI-6 (**done 2026-09-22**, e81b5a0: the upgrade break marks the tower used for the whole tick) AI-6 (2026-09-21, level-designer) Bot upgrade-break at capacity: under thinWalls (capacity ×0.8) a home that starts at exactly its capacity (L2 40/40) never upgrades — the bot re-links every tick and the sim only auto-upgrades an unlinked tower in `generation`; a human unlinking once upgrades at once. Fix: `UPGRADE_BREAK_SAFE_MS` must keep the tower unlinked through one whole tick (or issue the unlink one AI tick before re-linking). Levels 34/48 were retuned below the cap as a workaround — ai-engineer
- [x] AI-7 (**a/b/c done 2026-09-22**, e81b5a0: grow-first 8 s, shield reclaims its reinforcement, per-post artillery budget with clashes before guns, partial shields kept, 25 % landing-share; d documented in GDD §2.5 — remaining grower follow-up: under lean the L2 home never grows, and the bot abandons a capture one landing short to reinforce and cedes cheap contested neutrals with the +10 s contest penalty → AI-8) AI-7 (2026-09-21, level-designers) Rules v3 bot findings: (a) the bot never lets a lone L1 home grow to L2 before expanding, so every level ≥ 9 needed an L2 start — consider "grow home first" when facing ≥ 2 enemy towers; (b) it loses its last tower while its only link reinforces another (defend never reclaims a reinforcement for a shield); (c) the artillery model looks per-lane ("an L1/L2 stream into a gun post never lands"), which makes 41/42/47 wait for L3 (medians 90–120 s on 39–42, 46, 47, 50 vs the 60 s pacing target); (d) under lean a ×0.9 player never out-rates an equal-level enemy, so once an enemy barracks reaches L3 the map is a stalemate (levels pass only via fortress/artillery keeps or early wins) — ai-engineer + game-designer
- [x] ART-7 (**done 2026-09-22**, 35ee108: dashed 2 px / 45 % guide lines, ink-outlined enemy ribbons at 68 %, 11 baselines) ART-7 (2026-09-21, tech-artist) Readability under v3: guide lines (2 px, 30 % owner colour) vanish at 360 px — try 2 px / 45 % or dashed; enemy ribbons at 55 % over grass read tan rather than coral — add the 1 px ink outline the player's ribbons have or ~70 % alpha; keep the pixel-diff e2e baselines in the same commit — frontend + tech-artist
- [x] LV-13 (**done 2026-09-22**: level 1 enemy turtle instead of rusher, camp beside home at 0 units, foe 3 units at (250,830); bot 9.1 s, scripted human line 19–27 s; clock kept at 30/60 as a tutorial deviation from the 0.9 × median formula) LV-13 (2026-09-21, tech-artist) Level 1 star3 = 30 s is unreachable by the reference player (0/20, median 32.4 s) and a straight human line deadlocks against the shielding enemy — re-check the clock or the layout so a first-time player can 3★ the first level — level-designer
- [x] LV-12 (**done 2026-09-22**: after the AI-7 re-clock only 39 (106 s) and 47 (77 s) exceeded 75 s — 39 red/green fortress 12 → 4 (87.5 s), 47 yellow fortress 8 → 4 (64.9 s); every star2 off the 180 s cap; 20-seed clocks rewritten on 1–50, 33 kept at 35/70 for the shipped weekly pin) LV-12 (2026-09-21) Pacing under v3: 12 of levels 26–50 have medians above the 60 s GDD target (39–42, 46, 47, 50 at 90–120 s; star2 at the 180 s cap on 39–41, 50); winnable but slower than the pillar — re-clock after AI-7, then consider shorter lanes / fewer keeps — level-designer + game-designer
- [x] AI-5 (retired 2026-09-21: bridges no longer exist under rules v3) Reference player on two-bridge maps under lean (observed while tracing LV-10, not masked by the retune): rule 5 cuts the second bridge whenever a 1-hop detour exists, even when the detour tower is an L1 flanked by two enemies, and the crossing logic sends waves to both banks within ~7 s of each other; level 41 no longer punishes it, other two-bridge maps under lean might — check with `--twist lean --pool 28-50 --seeds 10`, add a "detour tower is contested" veto to the cut if a level shows it — ai-engineer
- [x] AI-8 (**done 2026-09-23**, 8919d7a: contest parity replaces the flat penalty, fortress off-by-one fixed, finish-the-capture 3 s, grow-home 8 s; lean pool 98.4 → 99.3 %) AI-8 (2026-09-22, level-designers) Reference player under lean never grows the L2 home (always linked); abandons a capture one landing from completion to reinforce a threatened tower (level 10 lean trace); the +10 s contest penalty cedes every cheap contested neutral — ai-engineer
- [x] LV-14 (**done 2026-09-23**: 26 factory L2/20 → lean 50/50, 30 east ford widened → 20/20, worst 66 s) LV-14 (2026-09-22, level-designer) After the stacking rule levels 26 (19/20) and 30 (18/20, worst win 143 s) miss the 95 % bar at 20 plain seeds (CI's K = 3 passes); retune both (30: the second loss is a long two-front siege) and re-run `npx tsx scripts/reclock.ts --only 26-30 --write` — level-designer
- [x] AI-9 (**done 2026-09-24**, 8de120d: hold cost modelled from the rival's walking column — ~0 on shipped levels, every constant trade rejected by the K=50 sweep; level-11 lean cascade traced to the tick and documented, not a model error) AI-9 (2026-09-23, ai-engineer) Watch items after AI-8: level 11 lean 47 → 44/50 (unexplained cascade), 15 plain median 37 → 44 s and 27 43 → 49 s (the flat contest penalty's one real job: a won contested neutral under the rival's fire freezes its source); a 5 s contest cost restores 15 and lean 47 but costs lean 14 — pick the trade with a 50-seed sweep — ai-engineer
- [x] LV-15 (**done 2026-09-24**, f4a95e5: one rock at the diagonals' crossing, mid L2 neutral 28, nW/nE 3 → naive 50/50 at K=50; level 3 first tap now finishes before the rusher can land) LV-15 (2026-09-23, level-designer) Level 8 is the weakest tutorial layout for the naive line (16/20 at K = 20): the 595 px cross lanes foeW–nE / foeE–nW let the rusher contest a first tap on some seeds; every rock tried also cut homes–base — revisit with a different wall shape — level-designer
- [x] GD-1 (**done 2026-09-24**, 90699cf: GDD §3 "Authoring for the first-time line") GD-1 (2026-09-23, game-designer) Rusher never piles on a target already falling, so three rusher towers open on three targets at t = 0 — a single bait cannot protect the first taps; document the working tutorial patterns (nothing inside the horizon at t = 0, tower-in-the-way, home pocket) in GDD §3 — game-designer
- [x] LV-16 (**decided 2026-09-25**, game-designer, GDD §3 "first-time line" 7–8: the ≥ 4-lane rule binds only towers a shielding enemy (aggression ≥ `DEFENCE_MIN_AGGRESSION` 0.35) can defend, band 1 stays < 0.35 so level 8's 3-lane `foeW`/`foeE` are ratified (floor ≥ 2 lanes), validator text unchanged; the star2 floor stays at the 4 s naive median — the 8 s line is a winnability gate only, 1★ progresses) LV-16 (2026-09-24, level-designer) Level 8's foeW/foeE have 3 lanes each after LV-15 (below the ≥ 4 guideline; the 0.2 rusher never shields so the naive line cracks them) — Game Designer to ratify or the designer finds a 4-lane variant; also the 8 s naive medians on 2, 5, 7, 8 exceed star2 (the floor is defined at 4 s) — accepted for now — game-designer
- [x] FE-2 (**done 2026-09-25**, 629b9cd: e2e/stalemate.spec.ts — level 2 seed 1, rusher attacks home at 12 s, hint once, never on level 1) FE-2 (2026-09-24, frontend) Stalemate hint (FE-1) resets from `landed` at frame end, so at ×20 speed the window restarts up to 500 ms late — harmless; e2e coverage for the hint (a scripted stalemate on level 2) still to add — qa-engineer
- [x] GD-2 (**decided 2026-09-25**, game-designer, GDD §3 "first-time line" 9: 23 and 32 stay learning levels — no growing home; band-3/4 rule: from 17 a level is a learning level when the reference player passes the gates, the lesson opens with the winning first verb and the stalemate hint fires on the contested lane; 23 already starts at L2/30 u and still loses every naive seed (parity race 4.i), so an L2 start is not the lever; re-open only on telemetry (attempts > 3 or a > 15 % completion drop at 23/32), then a pocket neutral, never an L2 start on 32) GD-2 (2026-09-25, level-designer) Levels 23 and 32 hold the naive line at 4/20 and 2/20 (lessons rewritten; layouts would need a growing home — L2 start or a pocket neutral — a band-3/4 rule call) — game-designer
- [ ] LV-17 (2026-09-25, QA-7) Lean twist on level 36 defeats the naive line in two weekly picks (2026-10-19, 2026-11-09: 1/5 and 0/5, 5/5 without the twist); 32-lean and 23-reinforced daily picks also lose (level, not twist) — informational until the weekly is meant to hold naive players — level-designer
- [ ] M3-5 Perf on a real mid phone: measure on the device (PERF-4 sprite cache shipped 2026-09-25: −22 % main-thread frame time at CPU 4× headless; `?spritecache=0` for an on-device A/B) — qa-engineer + mobile-engineer
  - 2026-09-18 (QA) **measured** — no phone, so headless Chromium (Pixel 5 viewport, software raster, no GPU) with CDP `Emulation.setCPUThrottlingRate` 4× as the mid-phone stand-in; production build, 20 s samples of rAF deltas (scratch specs in the session scratchpad, not committed):
    | run (level 40 seed 1, ×1) | median | p95 | max | units max / mean |
    |---|---|---|---|---|
    | autoplay bot, CPU 4× | 116.7 ms | 133.4 ms | 150.1 ms | 40 / 21 |
    | autoplay bot, CPU 1× (baseline) | 16.7 ms | 33.4 ms | 33.5 ms | 41 / 20 |
    | synthetic: `state.units` topped up to 400 marching units every frame (two owners, opposite directions → clashes + death particles), CPU 4× | 200.0 ms | 216.7 ms | 233.4 ms | 405 / 400 |
    | same synthetic, CPU 1× | 33.3 ms | 50.0 ms | 50.1 ms | 407 / 400 |
    The sim kept real time in every run (20.1–20.2 s sim in 20 s wall). Realistic simultaneous units in an authored level peak at ~40 (level 40 with the bot), an order below the 400 target.
  - Where the frame goes: CDP `Performance.getMetrics` at CPU 4× — level 40: script 11.5 ms of a 124.8 ms task (**9 %**); synthetic 400 units: script 32.1 ms of 212.4 ms (**15 %**). V8 profile (unminified build, 250 µs sampling, 37 k samples): 91.4 % `(program)` = native canvas rasterisation; JS self time is led by canvas calls (`quadraticCurveTo` 0.6 %, `fill` 0.5 %, `ellipse` 0.4 %, `lineTo` 0.4 %, `fillText` 0.3 %, `save` 0.3 %), then `drawGame` 0.3 %, `drawBadge` 0.2 %, `roundRect` 0.2 %, `cylinder` 0.2 %; **GC 0.2 %**; JS heap 3.4 → 5.9 MB over 15 s, no growth trend.
  - Allocations per frame (read, not profiled hot): `sim/step.ts` ≈ 30 small objects per tick (`state.events = []`, `boosters.filter`, two Sets in `generation`, `[...state.links]` twice in `pruneLinks`, one `linksFrom` filter per tower in `drain`, the `move` Map, Sets + `units.filter` on deaths in `hazards`/`clashes`/`arrivals`, an `unitPosition` point per unit per artillery tower) → ~600 objects/s at 20 ticks/s. `render/particles.ts`: one object per spawned particle (`make`), hard cap 700, dead ones swap-removed in `draw` → zero per-frame garbage (already pool-like). `render/draw.ts`: `unitDraws` makes one `UnitDraw` + one `Pose` per unit per frame and sorts, `spawnDust` a Map + template-string key per unit, `Object.values(state.towers).sort` per frame.
  - **Recommendation: do not pool.** Script is 9–15 % of the frame and GC 0.2 %; pooling sim/particle objects would win well under 1 ms of a 117 ms throttled frame. The cost is draw calls / fill rate in the dynamic layer (terrain and roads are already cached in `render/terrain.ts`). If MM-1 on a real device shows < 30 fps at ×1, levers in order: (1) pre-render tower sprites (`cylinder`, roofs, keeps — re-pathed every frame) per kind/level/owner into offscreen canvases and `drawImage` them; (2) cache badge text (`measureText`/`fillText` per tower per frame) per value; (3) HUD + booster bar on a layer redrawn only on change; (4) only then reuse `unitDraws` records. Item stays open for the on-device half (needs MM-1's APK on a phone); the measurement half is done.

## Next (M-mobile — iOS/Android)
- [ ] MM-1 Verify the Android debug APK from CI on a real phone; fix any WebView issue (safe areas, audio unlock, back button = pause) — mobile-engineer + qa
- [ ] MM-3 Signed release builds once keystore / Apple signing secrets exist (see docs/MOBILE.md) — mobile-engineer


## Later (M2 — content & feel)

## Later (M3 — meta & polish)
- [ ] M3-5 Performance: 400 units at 60 fps on mid phone (object pooling, dirty rects if needed)

## Later (M4 — release)
- [ ] M4-3 Release notes 1.0, post-launch backlog

## Bugs
- [ ] BUG-17 (2026-09-26, QA, levels / CI, **`npm run check` is red at HEAD**) `tests/levels/levels.test.ts:101` "LEVEL_META mirrors the level files (PERF-2 manifest is not stale)" — `expect(checkManifest()).toEqual([])` receives `src/levels/manifest.ts is stale (first difference at line 60: expected { id: 36, name: 'One Wide Ford', … star3: 45000, star2: 90000 …`. Cause: 5618eaf (LV-17) re-clocked `036-three-bridges.json` to 45 s / 90 s but did not regenerate the manifest, which still says `star3: 40000, star2: 80000` — the level map / result card read the stale clocks for level 36 (`LEVEL_META`) while the sim uses the JSON. Repro: `cd tower-clash && npx vitest run tests/levels/levels.test.ts`. Fix (Level Designer): `npm run levels:manifest` and commit `src/levels/manifest.ts`; CI's unit step fails the same way until then.
- [x] BUG-16 (2026-09-25, QA, e2e / weekly; **fixed 2026-09-26**, QA: `playwright.config.ts` `use.serviceWorkers: 'block'` for the whole suite + bounded steps `actionTimeout` / `navigationTimeout` 20 s, `expect.timeout` 15 s; regression test `tests/repo/hygiene.test.ts`) `e2e/weekly.spec.ts:246` "RETRY / restart after the Monday rollover" hit the 90 s test timeout once in three full-suite runs under load (build `index-Bx9ZzT9n`): `keyboard.press('r')` never acknowledged, screencast stops 22 ms after the press, `/assets/034-weakest-link-*.js` pending to the end. Root cause (2026-09-26, two playtest loops as load, load 5–12): **a renderer main-thread stall in Chromium headless_shell 1194 on a lazy level-chunk `import()` routed through public/sw.js, not game or spec logic.** Evidence: (1) the 034 request was issued by `enterLevel → whenIdle(preloadLevel(34))` 4 ms *before* the press (not by `goLevels` as first read) and the last screencast frame is still the play screen at 00:00 — the key was never handled, the rollover code never ran; (2) the same signature reproduced once in 230 boots of the exact steps on another build (`index-lsMnZTrt`) at a different step — PLAY tap → `loading`, `lazyScreens` served, level-33 preload `import()` issued 288 ms later, screencast stops 6 ms after it, the next `page.evaluate` never returns; (3) in both traces every response after the worker's `clients.claim()` (~150 ms after boot in normal boots) has no server IP/port and 0 transfer size, i.e. came from sw.js, and the never-answered request is the next worker-routed chunk; the main thread stopped emitting `Network.responseReceived`, input acks and `Runtime` replies at once, so the pending request is a symptom of the stalled thread, not a slow server; (4) ruled out: every loop on the `r` path and the map's first paint is constant-bounded (`GameLoop.advance` caps dt 250 ms / 10 ticks; AI loops monotone), SIGSTOP/SIGCONT of the renderer, GPU, network and browser processes for 3–8 s around the press always recovers (press acked right after CONT), stopping the worker mid-fetch (CDP `ServiceWorker.stopWorker` ×270) only aborts the import (`net::ERR_ABORTED`, the game toasts) with the main thread alive, the browser process closes in 54 ms at teardown (alive), 1,000+ instrumented boots (`Debugger.pause` + gdb watchdog) with no third hit; ≈ 1 hang per 500 boots. Fix: the suite never needs the worker (smoke only fetches `/sw.js` with `page.request`; lazy specs already blocked it), so every chunk now comes straight from the preview snapshot, and a stalled step fails in ≤ 20 s naming itself. Left open (low): the PWA offline path has no e2e; if the hang ever recurs with the worker blocked, the trace's `_serverPort`-less responses are the first thing to check.
- [x] BUG-15 (2026-09-25, QA, e2e infra / QA-6; **fixed 2026-09-25**, QA: `playwright.config.ts` serves a **snapshot of dist/** (`$TMPDIR/tower-clash-e2e/<port>/dist`, copied by the web-server command when the server starts, so a parallel rebuild can no longer change what a running suite serves), and `e2e/lazy.spec.ts boot()` navigates with `waitUntil: 'commit'`, bounds the debug-surface poll (10 s + 10 s per attempt) and retries the navigation once — a stalled boot now fails in ≤ 40 s naming the phase) `e2e/lazy.spec.ts` "menu chunk" timed out twice this week at `page.goto` (90 s, full-suite runs only). Root cause found under load (2 playtest loops, load average 5–9, three full-suite attempts): **another agent's `npm run build` / `npm run e2e` rewrote the shared `dist/` mid-run** (16:25:25, every content hash changed: `lazyScreens-BffMi0Eb` → `lazyScreens-Dtx9ipZ5`). Trace `pw-qa/lazy-…/trace.zip`: the page had booted on the old `index.html`; after `unroute` the re-fetch `lazyScreens-BffMi0Eb.js?r=…` and `skinShapes-Cm4qFvr3.js` came back **404** → `expect.poll(screen).toBe('levelSelect')` received `title` (the same rebuild made `smoke.spec.ts:565` "language" fail on the in-flight AZ text "toxun" vs "vur"). A boot that lands inside the rewrite window finds no `index-*.js` (or a 404 `index.html`), so `waitForFunction(__towerclash)` — unbounded, like `page.goto` (no navigation timeout in the runner) — sits until the 90 s test timeout: the "hang". Ruled out with evidence: the route intercepting the document (Playwright pauses every request with a route on and continues the non-matching ones server-side — Chromium `load` at 70–150 ms with the route on, 25/25 boots), the service worker (`serviceWorkers: 'block'` is an init-script stub of `register`, no interception; 12/12 sequenced boots after a SW-registering context passed), the idle preload (`load` fires ~1 s before the aborted lazyScreens request). Observed, not staged: attempt 2 of 3 under load coincided with a parallel agent's rebuild (attempt 1 and 3 green); after the fix a run serves the port's snapshot (71 JS files at server start) whatever happens to `dist/` meanwhile, and the lazy + smoke specs pass on the snapshot (PW_PORT=4193). Not a game bug; nothing for another owner.
- [x] BUG-14 (2026-09-25, QA, play screen, low) Stalemate hint blind spot: when the counter-stream's units clash on their spawn tick (level 2 seed 1: home→side @500, unlink @14800, home→mid @14900) every player unit dies at home on the tick it spawns, `laneStalemate` never sees an outbound unit and the hint never fires although nothing lands; the same link at 15000 fires at 23000. Fix: `unitDied` carries `roadId`, the watch counts player clash deaths on the link's lane as an opposing stream — **fixed 2026-09-25** (frontend)
- [x] BUG-1 (2026-09-13, QA) `e2e/smoke.spec.ts` fails after the save-schema v2 change — CI will go red once `src/ui/save.ts` is committed. Owner: Frontend Engineer (spec file is theirs today).
  - Repro: `cd tower-clash && npm run e2e -- smoke.spec.ts` → `smoke.spec.ts:191` `expect(saveAfterWin?.stars['1']).toBe(stars)` — Expected 3, Received undefined.
  - Cause: `src/ui/save.ts` now writes to `SAVE_KEY = 'towerclash.save.v2'` (v1 is only read for migration), but the spec mirrors `const SAVE_KEY = 'towerclash.save.v1'` (line 33), so `readSave` returns null. Verified the game is correct: after an autoplay win localStorage `towerclash.save.v2` = `{"version":2,"stars":{"1":3},"coins":30,...}` and `getResult()` = `{won, 3 stars, 30/30 coins}`; `tests/ui/save.test.ts` passes.
  - Fix: bump the mirror to `towerclash.save.v2` and also assert `localStorage.getItem('towerclash.save.v1') === null` on a fresh save (regression guard for the key move). Consider exposing `getSave()` on `window.__towerclash` so e2e stops mirroring the key.
  - 2026-09-13 (later, QA): recurs with schema v3 — `src/ui/save.ts` now writes `towerclash.save.v3` with `gold`, while `e2e/smoke.spec.ts:37` still mirrors `towerclash.save.v2` / `coins` (`readSave` will return null again, `saveAfterWin?.stars['1']` undefined). Same fix; exposing `getSave()` on the debug surface ends this class of break for good.
  - 2026-09-14 (QA): **fixed** — `smoke.spec.ts` mirrors `towerclash.save.v3`/`gold` and `window.__towerclash.economy.getSave()` exists; smoke passes on a clean HEAD worktree and in today's sweep (regression guard = the smoke spec itself).
- [x] BUG-2 (2026-09-13, QA, hygiene) `tower-clash/.gitignore` ignores only `.env*.local`; the Android/iOS workflows now write `tower-clash/.env.production` from repository secrets (`VITE_RC_*_KEY`, `VITE_ADMOB_*`). A developer who creates the same file locally (docs/MOBILE.md §8.3 suggests `.env.local`, but Vite also reads `.env.production`) would commit store keys. Owner: Mobile Engineer (`.gitignore`/MOBILE.md are theirs).
  - Repro: `cd tower-clash && echo 'VITE_RC_ANDROID_KEY=goog_x' > .env.production && git status --short` → `?? .env.production` (not ignored).
  - Fix: add `.env.production` (or `.env*` minus `.env.example`) to `tower-clash/.gitignore`; mention in MOBILE.md §8.3 that CI writes it.
  - 2026-09-14 (QA): **fixed** — `.env.production` is in `tower-clash/.gitignore`. Regression test: `tests/repo/hygiene.test.ts` (fails if the ignore lines for `.env.production` / `.env*.local` disappear).
- [x] BUG-3 (2026-09-13, QA) Working tree (uncommitted, Frontend Engineer's save-v3 / economy work in flight): `SaveData.coins` was renamed to `gold` in `src/ui/save.ts` and `src/ui/boosters.ts` but the callers were not updated, so `npm run check` and `npm run e2e` fail and **every level is unplayable in the browser** (sim never ticks). Must be fixed in the same commit; CI (`tower-clash-ci.yml`) goes red otherwise. Owner: Frontend Engineer.
  - `npm run typecheck`: 19 errors — `src/main.ts:194`, `src/ui/play.ts:152,171,214,232`, `src/ui/screens.ts:86,152,275` (`Property 'coins' does not exist on type 'SaveData'`; `TitleOpts`/`LevelSelectOpts` have no `coins`), `tests/ui/save.test.ts:34–114` (9×).
  - `npm run test`: `tests/ui/save.test.ts` 5 failed — `recordWin … pays 10 per new star` (`save.test.ts:34` expected undefined to be 20), `normalizeSave clamps hostile input` (`:52`), `reads a v1 save … writes the v2 copy` (`:79` expected 3 to be 2 — version is now 3), `prefers the v2 entry …` (expected undefined to be 99), `spendCoins only deducts what is affordable`.
  - Runtime (vite build without tsc, then Playwright): `loadLevel(1)`/`loadLevel(40)` → screen `play` but `getState().time` stays 0; `pageerror: TypeError: Cannot read properties of undefined (reading 'prices')` at `boosterStatus` (`src/ui/boosters.ts:53`) ← `PlayScreen.buildUi` (`src/ui/play.ts:152` passes `this.app.save.coins`, now `undefined`, so `walletOf(undefined)` returns undefined) ← `draw` ← `frame`; the exception aborts every frame. `e2e/perf.spec.ts` "level 40 … median frame" fails with `sim must have advanced while sampling` (Expected > 0, Received 0).
  - Fix: pass `this.app.save.gold` (or a `BoosterWallet` with `charges`/`prices`) from `play.ts`, update `main.ts`/`screens.ts` and `tests/ui/save.test.ts` to `gold`, and make `walletOf` tolerate `undefined` (`?? { gold: 0 }`) so a wallet plumbing mistake can never freeze the game loop again. Regression test to add once fixed: `tests/ui/boosters.test.ts` — `boosterStatus(state, kind, undefined as never)` does not throw; e2e content spec already asserts `time > 0` per level.
  - 2026-09-14 (QA): **fixed** in 5dc94e9 — `npm run check` green (327 tests), `content.spec.ts` asserts `time > 0` on all 40 levels. Residual (low, still open): `walletOf` (`src/ui/boosters.ts:47`) still returns `undefined` for an undefined wallet, so `boosterStatus(state, kind, undefined as never)` throws `reading 'prices'`; the regression test from this entry is held back until the `?? { gold: 0 }` guard lands (QA adds the `tests/ui/boosters.test.ts` case the same day).
  - 2026-09-18 (QA): **residual closed** — `walletOf` (`src/ui/boosters.ts`) now returns `{ gold: 0 }` for an undefined wallet (one-line guard, done by QA with the test since it only exists to make the regression test possible); `tests/ui/boosters.test.ts` "BUG-3 regression: a missing wallet reads as no gold instead of throwing inside the draw loop" asserts `boosterStatus`/`allBoosterStatus`/`canUseBooster` with `undefined as never` — verified by `npm run check` (523 tests).
- [x] BUG-4 (2026-09-14, QA) `e2e/economy.spec.ts` "commander upgrade bought in the shop changes state.modifiers of the next level" **fails at HEAD (ac35c5f)** — the `tower-clash-ci.yml` e2e step is red on the branch. Owner: Frontend Engineer (a fix is already in their uncommitted `economy.spec.ts`, +163/−13 — commit it).
  - Repro: `cd tower-clash && npm run e2e -- economy.spec.ts` → `economy.spec.ts:76` `expect.poll(async () => (await save(page)).upgrades.production).toBe(1)` — Expected 1, Received undefined (10 s poll). Verified on a clean HEAD worktree: economy+smoke = 1 failed / 4 passed.
  - Cause: aec604e retuned the ladder (`src/economy/catalog.ts`: production maxTier 3, `TIER_COSTS_3 = [200, 350, 550]`, perTier 0.02) and updated the unit tests, but the spec still mirrors `TIER1_COST = 60`, `PRODUCTION_PER_TIER = 0.04` and seeds `gold: 100`. The tap on row 0 does reach `tapUpgrade` → `buyUpgrade` returns false (unaffordable, toast "Need 200 gold"); row 1 (Capacity, 60) buys fine — verified with a direct probe on the isolated build.
  - Fix: mirror the new ladder (seed ≥ 200 gold, or buy Capacity tier 1 → `capacityMul: 1.05`). To survive the next retune, stop mirroring: expose `economy.upgradeCost(id)` on the debug surface and read it in the spec.
  - 2026-09-18 (QA): **fixed** in 95a1a23 — the spec buys the Capacity row (`CAPACITY_ROW = 1`, `TIER1_COST = 60`, `CAPACITY_PER_TIER = 0.05`) instead of the retuned Production row; verified by `cd tower-clash && npm run e2e -- economy.spec.ts` at HEAD dfe349d (8 passed, 1.0 min; run on a private preview port with the same config). Regression guard = the spec itself; the `economy.upgradeCost(id)` debug hook suggested above is still worth adding before the next retune.
- [x] BUG-5 (2026-09-14, QA) Working tree (uncommitted achievements work in `src/main.ts` / `src/economy/achievements.ts`): `e2e/smoke.spec.ts:280` `expect(result).toEqual({ outcome, stars, coinsEarned, coinsTotal, crystalsEarned })` will fail once `main.ts` is committed — `getResult()` now also returns `achievements: ['first_win', 'flawless', 'speedrunner']` (`+ Received + 5`). Retried after 5 min on a fresh isolated build: still failing. Owner: Frontend Engineer (both files theirs — same commit).
  - Repro: `cd tower-clash && npm run e2e -- smoke.spec.ts` on the current tree.
  - Fix: pin the new key in the spec (`achievements: ['first_win', 'flawless', 'speedrunner']` for the 3★ first clear on seed 1 — it is content, so assert it rather than `expect.any(Array)`), and keep `crystalsEarned` consistent with whatever the unlocks pay.
  - 2026-09-18 (QA): **fixed** in 95a1a23 — `smoke.spec.ts` asserts `getResult()` with `toMatchObject` (the `achievements` key is tolerated) and `economy.spec.ts` asserts `save.achievements.unlocked`; verified by `cd tower-clash && npm run e2e -- smoke.spec.ts economy.spec.ts` at HEAD dfe349d (10 passed). The seed-1 first clear now reports `achievements: ['first_win', 'speedrunner']` (no `flawless` after the v2 retune); smoke pins `first_win` from today.
- [x] BUG-6 (2026-09-14, QA, hygiene) Untracked `tower-clash/e2e/look2.tmp.spec.ts` (Tech Artist screenshot scratch: 3 tests, no assertions, absolute `SHOTS` path) lives in Playwright's `testDir` and matches the `chromium` project, so `npm run e2e` and CI would execute it the moment it is committed (`git add -A`). Owner: Tech Artist — delete it before committing, or keep scratch specs outside `e2e/` (e.g. the session scratchpad); files that must stay should not end in `.spec.ts`.
  - 2026-09-18 (QA): **fixed** — the file was never committed and is gone from the working tree: `git ls-files | grep -i 'look2.*spec'` → empty, `ls tower-clash/e2e/*.tmp.spec.ts` → none. Regression test: `tests/repo/hygiene.test.ts` "tower-clash/e2e (BUG-6)" fails on any `*.tmp|scratch|wip|local.spec.ts` under e2e/, on a spec without an `expect(`, or on a hard-coded absolute output path.
- [x] BUG-7 (2026-09-18, QA, balance) Level 16 Last Bastion under the **Lean** twist (`productionMul` 0.9) wins only **30/50 = 60 %** for the reference player — below the GDD §7.5 item 3 pool gate (≥ 80 %). It passes the CI twist gate at K = 5 only because seeds 1–5 give 4/5 (seed 5 lost), so the gate is green but the level is not robust; the day Lean lands on level 16 the daily fixed seed has a 40 % chance of being unwinnable for the bot (no such day in the next 60 from 2026-09-18 — `--daily 2026-09-18 --days 60 --seeds 3` is 0 failed). Owner: Level Designer (level file) — or AI Engineer if the lean loss pattern is a reference-player gap.
  - Repro: `cd tower-clash && npm run playtest -- --twist lean --seeds 50` → `16   Last Bastion   30/50   0/30/0/20   34.5s   40.3s   FAIL   5,11,13,17,18,19,20,21,27,30,31,32,36,39,42,44,45,47,49,50` and `FAIL lvl 16 Last Bastion [lean]: 30/50 < 80% (lost …)`; exit 1. Every loss is a 0★ (the bot is wiped, not slow).
  - Control: `--twist plain --seeds 50` → 16 wins 50/50 (median 28.6 s); `--twist thinWalls --seeds 50` → 50/50; campaign `--level 16 --seeds 50` → 50/50, idle never wins. Only Lean fails, so the −10 % production is what tips it: the level's opening is a production race with no slack.
  - Fix: retune level 16 for the pool as 13/15/27/31 were (c2353a1, b52491d) — a slightly smaller enemy home garrison or a later enemy clock — until `--twist lean --seeds 50` ≥ 40/50; the 50-seed lean row for level 16 is the regression check (add it to the level's commit message like the earlier passes).
  - 2026-09-18 (Level Designer): **fixed** — `src/levels/016-last-bastion.json` enemy `camp` barracks 6 → 5 (one knob; lesson, names, personalities, translations and clocks unchanged). Root cause (trace, lean seed 5): the bot takes west/east by 4 s and by 17 s holds seven towers at 0–1 units each; the enemy `camp` grabs `ne` at 4.8 s and, at 6 units, still refills fast enough to feed a second wave `ne → east` at ~22 s while the keep (25 units by 20 s) retakes `guns`; with −10 % production the bot's home is at 0–2 units and the retake cascades east → nw → west → home (lost at 32.7 s). At 5 units the camp empties itself on `ne` (5.8 s, 1 unit left at 15 s), so there is no second wave and the bot converges on the keep as designed. Shrinking the keep (10 → 8/9) or growing home (15 → 16/17) made lean *worse* (25–29/50: the bot rushes the keep earlier and overextends), so the fix is the flank, not the keep. Numbers at 50 seeds (`--twist <id> --seeds 50`): plain 50/50 (median 28.6 → 26.1 s, −8.7 %, under the 10 % clock re-fit band), lean **50/50** (was 30/50; 100/100 at 100 seeds), fastFeet 50/50, thinWalls 50/50, reinforced 50/50; pool under lean 1573/1600 = 98.3 %, 0 failed. `npm run playtest -- --seeds 20` 0 failed gates (16: 20/20, idle never wins); `--upgrades max --seeds 10` median 2.0★ ≤ 2.5, 0 failed; `--daily 2026-09-18 --days 60 --seeds 5` 0 failed days (16 lands on 2026-11-08 reinforced, 5/5); `npm run levels:check` 40 valid; `npx vitest run tests/levels tests/ai/daily.test.ts` 66 passed. Also below 45/50 at K = 50 (lean only, still above the 80 % gate, not touched today — candidates for a follow-up retune): 19 Toll Road 44/50 (lost 5,7,13,26,44,48), 20 Roadblock 43/50 (lost 6,8,9,19,23,24,39); plain, fastFeet, thinWalls and reinforced have no level under 49/50.
- [x] BUG-8 (2026-09-19, QA, lazy chunks; **fixed 2026-09-19**, Frontend: `src/lazyChunk.ts` wraps every `import()` — a failure that names the chunk URL (Chromium/Firefox) is re-fetched under `?r=<time>` on the next tap, draw-time retries back off 3 s, the returning screen shows `common.loadFailed`; a chunk that keeps failing without a URL (Safari) reloads once per session from the title / map only (sessionStorage guard). `e2e/lazy.spec.ts` now pins the recovery of all three chunks, `tests/ui/lazyChunk.test.ts` the helper; to pay for it `drawThemeSwatch` / `drawUpgradeGlyph` (shop + level map only) moved from terrain.ts / sprites.ts into the lazy `render/spritesShop.ts` — eager bundle 79.8 → 79.3 kB gzip) A level chunk (PERF-2 `import()` per level) or the skin chunk (M3-2 `skinShapes`) that fails to download once stays **unreachable for the life of the page**: the browser's module map records the failed fetch, so every later `import()` of the same URL rejects at once without a request — `levels/index.ts` "a failed download is forgotten so the next call retries", `main.ts loadLazy` and `sprites.ts loadShapeSkins` all retry in vain. The game is not stuck (the tap returns to the screen it came from, the level draws the default look) but the player gets **no feedback** and the level / skin never opens until a reload. Owner: Frontend Engineer (`src/main.ts`; `levels/index.ts` is shared).
  - Repro: `cd tower-clash && npm run e2e -- lazy.spec.ts` — "level chunk" test: abort `/assets/003-*.js`, `loadLevel(3)` → false, unroute, `loadLevel(3)` → **false again** (annotation `level 3 after the network came back: loadLevel → false`); the spec pins the current behaviour with a pointer here so it stays green — flip `expect(await …levelId).toBe(retry ? 3 : 1)` to `toBe(3)` when fixed. Same for `skinShapes`: after the network is back no new request is ever made (`page.waitForResponse(/skinShapes-/)` times out).
  - Fix options: listen for Vite's `vite:preloadError` on a *user-initiated* navigation and reload from the title / map (never during a match; the idle preloads must keep swallowing), or re-fetch under a cache-busting URL (`import(/* @vite-ignore */ url + '?r=' + n)` needs the manifest to expose the URL), plus a toast (`t('common.offline')` or similar in all four locales) on the failed tap. Regression test to add once fixed: the `lazy.spec.ts` line above plus `page.waitForResponse` for the skin chunk after `unroute`.
  - Also found and **fixed** today (QA, obvious guards): `resizeLayers` reset a layer's cache key without clearing its pixels, so a same-size resize (`orientationchange` before the metrics change, `visualViewport` echo) followed by pause / result / level map left the stale HUD on the top canvas above the menu — 57 733 of 255 300 sampled HUD pixels painted while paused on the unfixed build (`e2e/layers.spec.ts` "same-size resize + pause", `tests/render/layers.test.ts`). `loadShapeSkins` kept a rejected promise forever (`tests/render/skinChunk.test.ts`) — harmless in the browser because of the module map above, but consistent with `loadLazy` now.
- [x] BUG-9 (2026-09-19, QA, daily, low; **fixed 2026-09-19**, Frontend, producer's call: `restartLevel` in `src/ui/daily.ts` re-reads `app.dayKey()` on RETRY / restart and goes to the level map with the `daily.newReady` toast when the day changed — `tests/ui/daily.test.ts`, `e2e/daily.spec.ts`, GDD §7.4) After the UTC rollover the result screen's RETRY and the pause menu's restart (`PlayScreen.restart`, `ResultScreen.retry`) replay the **stale day's** challenge (the `DailyChallenge` object bound at start), and a win then books against yesterday — GDD §7 says the reward "stays available until 00:00 UTC", so a player who starts at 23:59 and retries at 00:01 can still collect D's reward (and keep the streak) while the map already shows D + 1. Not a crash; deterministic (`tests/ui/daily.test.ts` "RETRY after the rollover replays the stale day" documents it). Owner: Frontend Engineer, with the Game Designer's call: either RETRY re-reads `app.dayKey()` and starts today's challenge when the day changed (toast "New challenge"), or the GDD wording changes to "a match started before 00:00 UTC counts for that day, retries included".
- 2026-09-21 (QA) Correctness review of the weekly / DAILY-4 / PERF-4 sprint (`git diff 64f6e01..HEAD -- tower-clash/src`), no new bug to file. Confirmed and **fixed as an obvious guard**: `isMondayKey` (`src/daily/challenge.ts`) and `mondayString` (`src/ui/save.ts`) accepted rolled-over dates — V8 parses `2026-02-30T00:00:00Z` as Monday 2026-03-02, so a hostile `weekly.lastWinWeek` / `weekly.best` key like `2026-02-30` survived normalisation and `weeklyFor('2026-02-30')` returned a challenge distinct from the real week's; both now require a real calendar date (round trip through `toISOString`). Not reproducible: double payment when the daily and the weekly pick the same level on the same day (2026-11-09 → level 36 twice: separate ledgers, each pays once, campaign stars / milestones / achievements / defeats untouched, the later campaign clear is still a first clear — `tests/ui/challengeLedgers.test.ts`), the weekly streak on hostile saves (not clamped, like `challenge.streak`; shown capped at 99, `+1` stays finite, JSON round trip is a fixed point), the daily/weekly blocks cross-reading each other's fields, a "best" line on a lost weekly result (the note is inside the won branch). Picker pins confirmed equal to GDD §7.2 (44/44606/Lean, 31/711749/Reinforced, 21/489368/Thin walls; weekly 33/743047/Fast feet/0:40) and now asserted exactly with cycle-boundary (2026-10-21 → 2026-10-22, cycle −1) and leap-day / pre-epoch `dayNumberOf` cases in `tests/daily/challenge.test.ts`. `tests/ui/i18nKeys.test.ts` did not scan `t(cond ? 'weekly.x' : 'daily.x')` (typed, so no missing key, but the cast guard had a hole) — the scan now covers ternaries. Lazy cosmetics: `e2e/lazyCosmetics.spec.ts` (roof_pagoda + unit_robots + theme_neon, sound on, 3 s of level 1: no pageerror / console error, skinShapes / themes / recipes chunks fetched exactly once, SFX chunk warmed by the first tap) passes. CI gates measured 2026-09-21: daily 30×3 = 2.5 s, weekly 26×5 = 4.0 s, twists 5×42×5 = 24.0 s — 30.5 s total, all 0 failed; the workflow's stale "32 levels / 9–40 / 3 seeds" text refreshed.

- [x] BUG-10 (**fixed 2026-09-22**, 35ee108: `decideLink` → `empty`, `hint.empty` in 4 languages) BUG-10 (2026-09-22, QA, play screen, low) Tapping (or dragging) from a selected **own tower at 0 units** onto a connected tower gives no feedback: `decideLink` (`src/input/pointer.ts`) does not know the garrison, returns `link`, the command goes to the sim and `link()` drops it silently (rules v3 rule 6: a stream needs soldiers). No shake, no hint — the limit hint ("L2 needed…") would be wrong there. Repro: `tests/ui/pointer.test.ts` "a tap from an own tower at 0 units issues a link the sim refuses … BUG-10" (documents the current behaviour: command emitted, `state.links` empty, `limitHint` null). Fix: a `hint.empty` ("Qala boşdur" / "No soldiers" / RU / TR) in all four locales and a `{ kind: 'empty' }` decision in `decideLink`. Owner: Frontend Engineer (pointer.ts + i18n).
- [x] BUG-11 (2026-09-22, QA, sim; **fixed 2026-09-22**, QA: `src/sim/step.ts generation()` checks capacity / `tryAutoUpgrade` before the freeze and under-fire gates — 3-line reorder, one-line justification in the comment) A tower **at capacity that is under fire or frozen** (not linked) did not auto-upgrade until the fire / freeze ended — GDD §2.2 says "on the spot", and §2.0b says under fire pauses *growth only*. Repro (before the fix): L1 at 25 linked, hit by one hostile landing, reinforced back to 25, `unlink`, `step` → still L1 until `underFireUntilMs` passed (1.5 s after the last landing — forever under a continuous stream). Consequence for the AI: the *upgrade break* (`tactics.ts maintain`: drop every link for one tick to take the level) never took the level while the keep was under fire, so a bot keep at 25/50 under a hostile stream re-linked and unlinked every AI tick and emitted nothing. Regression tests: `tests/sim/linkedUpgrade.test.ts` "upgrades it even while it is under fire" and "upgrades a frozen tower too". Balance check: `--seeds 3` 50/50; lean twist 0 failed, daily 0 failed, weekly unchanged vs HEAD (the 34-lean 2/3 week fails identically without the fix; 47/50 at K = 50 both ways).
- [x] BUG-12 (**resolved 2026-09-22**: the AI work landed lint-clean in e81b5a0 and the gates re-derived with the clocks) BUG-12 (2026-09-22, QA, working tree) Uncommitted `src/ai/{common,index,referencePlayer,tactics}.ts` (AI Engineer, in flight) currently fail `npm run lint` (4 × `@typescript-eslint/no-explicit-any`: `referencePlayer.ts:89`, `tactics.ts:169/174/240`) and, on the same tree, the playtest gates: daily 1 failed day (2026-10-03 lvl 10 lean 2/3), twist lean 4 failed levels (41 Twin Rivers 2/5, 47 Scorched Earth 3/5, +2), weekly 2 failed weeks (36 lean fixed seed still *playing* at 180 s, 34 lean). All of these pass on a clean HEAD worktree (with or without BUG-11's sim fix), so they belong to the in-flight AI change — must be green before it is committed. Owner: AI Engineer.
- [x] BUG-13 (**fixed 2026-09-23**, level-designer: 2/4/6/7/8/13 re-laid so the naive line's first tap is uncontested — levels 1–16 pass the naive gates; star2 on 1–8 floored at the naive median via scripts/reclock.ts) BUG-13 (2026-09-23, QA, tutorial band / naive line, design) The **naive human line** (`npm run playtest -- --naive --seeds 5`, QA-3: every 4 s each own tower streams to its nearest non-own tower, never unlinks, never boosts) does **not** win levels **2, 6, 8** at react 4000 ms (1/5, 0/5, 1/5) nor **2, 4, 6, 8** at react 8000 ms (1/5, 0/5, 0/5, 1/5); levels 1, 3, 5, 7 hold (5/5, 4/5, 5/5, 3/5). Informational (no gate) but the band is meant for a first-time human. Owner: Level Designer (levels) / Game Designer (whether the tutorial should teach "switch a dead stream"). Traces (seed = first losing seed):
  - **Lvl 2 Supply Line** (seed 2, both delays): the rusher opens `foe → side` (740 px) at 0.0 s; the naive line's first tap at 3.0 s is `home → side` too (278 px, nearest neutral); the enemy captures `side` at 8.2 s; from then on the player's 1/s lands hostile on `side` while the enemy's 1/s reinforces it — `side` sits at 0–1 forever, `home` is frozen at 15 by its only (L1) link and never re-targets `mid`. Stalemate to 180 s. Fix idea: make the enemy's opening stream go to `mid` (or wall `foe–side`) so the player's nearest neutral is not the enemy's first target.
  - **Lvl 4 Build Up** (seed 1, react 8000 only): `home → outpost` at 3 s freezes `home` at 18; the turtle grows `foe` to L2 by ~25 s, streams `foe → home` (22 s) and `foe → mid` (24.5 s) at 1.43/s; `home` (under fire, no regen, 18 units) falls at 43.1 s, `outpost` at 63.6 s. The slower hand never lets a tower grow to L2, so the enemy out-rates every lane. At react 4000 it holds (4/5).
  - **Lvl 6 Outpost First** (seed 1, both delays): the naive line grabs the three neutrals (`east` 9.8 s, `mid` 17 s, `west` 25 s) and links every tower it owns, so all four player towers are frozen at L1 while the turtle grows `base` and `outpost` to L2 (19 s / 30 s); from 22 s `outpost → home`, `outpost → east`, `base → east` at 1.43/s take `east` (37 s), `home` (43.9 s), `west` (67 s), `mid` (74 s). The level's intended line (hit `outpost` first, 617 px) is never chosen by nearest-first play.
  - **Lvl 8 Two Bases** (seed 1, both delays): `foeW → nW` (0.5 s) beats `homeW → nW` (3 s) to the neutral at 8.7 s; `homeW` then streams into enemy `nW` for the rest of the match (frozen at 19), the rusher's three towers take `mid` (14.6 s) and grow `nW` to L3 (100 s); the player's east side (`nE`, `mid`, `foeE`) is taken back one by one, lost at 109.9 s.
  - Common cause: a lone L1 stream into a tower the enemy also streams into is a permanent 0/1 stalemate and the naive line never unlinks, so any level whose nearest neutral is also the enemy's opening target is lost on the first tap. Re-run after any level change: `npm run playtest -- --naive --seeds 5` (and `--react 8000`); target: levels 1–8 all `holds = yes`.

## Icebox
- Replays (command log is already deterministic)
- Local 2-player on one screen

## Done
- 2026-09-25 Day 13b (4e1235f, 2091271, ae0cf4a): FE-3 'How to play' card (5 illustrated rules, settings + pause, 4 languages); L10N-1 Azerbaijani pass (78 strings, toxun/qüllə glossary); GD-2 decided; QA-6 e2e dist snapshot per port + bounded lazy boots (BUG-15 root cause: parallel build rewrote dist), BUG-16 filed; QA-7 --naive on daily/weekly (12/14 days, 6/8 weeks hold).
- 2026-09-25 Day 13 (58917ec, e0674dc, 80308fb, 3dc4e19, 629b9cd + BUG-14 fix, store set): FE-2 stalemate e2e + naive CI gate 1–16; PERF-4 tower sprite cache (−22 % frame time, pixel diff ≤ 0.08 %); lessons 23/32 rewritten after the 17–50 naive sweep (47/50 hold); LV-16 decided; release-readiness pass (checklist statuses on build 6, §0 how-to-release EN/AZ, README AZ v3); store screenshots re-rendered on build 6.
- 2026-09-24 Day 12 (62bf578, ac41e5d, acf0789, 90699cf, f4a95e5, 8de120d): FE-1 stalemate hint (sim `landed` event + `laneStalemate`, toast once per link in 4 languages); LV-15 levels 8 and 3 uncontested first taps (naive 20/20); GD-1 GDD §3 first-time authoring guide; AI-9 hold cost + level-11 trace; version 1.0.0 build 6 + release notes. Clocks unchanged by reclock. Gates: 822 unit, playtest 50/50, daily, weekly, five twists, upgrades=max, naive, e2e, bundle.
- 2026-09-23 Day 11 (60fcd1f, 8919d7a + levels): QA-3 naive first-time line (`playtest --naive`) found BUG-13 — tutorial levels 2/4/6/8 not winnable by a simple player; 2/4/6/7/8/13 re-laid (uncontested first tap, walls hiding keeps from their outposts), 1–16 pass the naive gates; star2 on 1–8 floored at the naive median (reclock rule); AI-8 (contest parity, finish-the-capture, grow-home); LV-14 (26 L2 factory, 30 wider ford); QA-4 PW_PORT/PW_OUTPUT; 20 clocks re-derived. Gates: 789+ unit, playtest 50/50, daily, weekly, five twists, upgrades=max, e2e, bundle.
- 2026-09-22 Day 10 (b75a618, e81b5a0, 35ee108, b8d22fc + levels): BUG-11 sim fix (full tower upgrades under fire); AI-6/AI-7 bot (grow-first, artillery budget, shield reclaim, upgrade break); ART-7 readability; BUG-10 empty-tower hint; DOCS-2 + GDD §2 v3 sweep + designer's read (SHIELD_MS proposal); 48 QA edge-case tests; LV-13 level 1 rebuilt; LV-12 pacing (39, 47) and all 50 clocks re-measured at 20 seeds; 10 and 27 lean retuned. Also: stacking rule (idle towers join a siege; turtle only among enemies), grow-first only against ≥ 2 enemy towers, `scripts/reclock.ts` (clocks from 20-seed medians with the max-upgrade boundary step; 44 levels rewritten a second time). Gates: 783 unit, playtest 50/50, daily 30×3, weekly 26×5, five twists, upgrades=max ≤ 2.5, e2e 84 + 4, bundle ≈ 76 kB.
- 2026-09-21 RULES-3 (9c193ec…) **Rules v3 "free lanes"** shipped inside the 1.0.0 RC: sim (568b8b5: lanes from geometry with wall/water/rock obstacles and third-tower blocking, point mines, streams emit at the tower's production rate without draining, `sourceEmpty`, no bridges/barriers/queues; `link` needs ≥ 1 soldier), AI (1c4ec7c: lane-flow threat model, siege/reinforce plans, shields, personalities + reference player; DEFENCE_MIN_AGGRESSION 0.35), render/ui (40c6c62: obstacles + mines on the terrain layer, straight ribbons, guide lines, `hint.blocked` ×4, bridge cut removed), levels (c582f12 conversion + cfdcc85 / 28551dd retune: ≥ 4 lanes into every keep, L2 home from level 9, L3 on 48, fords/walls reopened, clocks at 20 seeds), QA (e2e 84 = 82 + guide-line/hold/blocked-tap specs, 33 baselines, CI comments), store set re-rendered (0e0ee66), docs (GDD §2.0b/§2.5/§3, skills, release notes, store texts, README, plan). Gates: 716 unit, playtest 50/50 ×3, daily 30×3, weekly 26×5, five twists 42×5, upgrades=max, bundle 75.7 kB. Follow-ups filed: AI-6, AI-7, LV-12, LV-13, ART-7.
- 2026-09-21 LV-10 (ef2769d) level 41 Twin Rivers retuned: bridgehead neutrals 10, banks 8, ford 6, keeps become artillery; lean 1500/1500 seeds, 180-day daily sweep 180/180 (the 630925 stall was a two-bridge cut + split waves — AI-5 filed).
- 2026-09-21 PERF-5 (8a63a69) equipped lazy cosmetics preloaded at level start (`render/cosmetics.ts`, skinned first frame), ground layer keyed on the resolved theme and blanked off-play, letterbox fills cover the rounded canvas edge, drawLock/drawFlag in the lazy menu widgets; bundle 78.7 kB gzip.
- 2026-09-21 MM-5 + PUB-10 (b43cfb1) version 1.0.0 / build 5 synced (package, Android, iOS); v1.0.0 RC release notes name levels 41–50 and the weekly challenge, store texts say 50 levels, post-launch roadmap shifted (levels 41–50 and the weekly challenge moved from 1.0.1/1.1 into 1.0.0).
- 2026-09-21 QA (0cb8d38) weekend diff review: real-calendar Monday keys, ternary-aware i18n key scan, daily+weekly same-level ledger tests, exact picker pins, lazy-cosmetics e2e, CI gate text refreshed; 701 unit / 86 e2e, all gates green.
- 2026-09-20 WEEKLY-1 (6399883, b6b77ad, 3f46f16) Weekly Challenge shipped: Monday 00:00 UTC, level 33–50, non-plain twist, 100 gold + 20 crystals at ≤ star3, week streak, DAILY/WEEKLY tab; `playtest --weekly` (26 weeks 26/26) + CI gate; DAILY-4 picker per-cycle shuffle, pool 9–50, 60-day sweep 60/60 (level 46 hardened a34522c).
- 2026-09-20 PERF-4 (8e8b55e) roof/helmet materials, cosmetic themes, SFX recipes, coin/crystal bursts in lazy chunks: eager 80.15 → 77.1 kB gzip (78.5 kB with the weekly), pixel diff 0 on the default look.
- 2026-09-20 LV-9 (bb5c147) band 5 "Grand Campaign" levels 41–50 authored, translated, clocked: 50-seed plain 500/500, every twist ≥ 47/50, max-upgrades gate ok; validator band 41–50. Frontend (d2086ae): level count data-driven (map, unlock, NEXT, achievements), daily card names the 00:00 UTC reset; Economy: band [41,50] 20 crystals, milestone 50 = 60, `grand_campaign` 10.
- 2026-09-20 M4-3 (8d279c9) v1.0.0 RC release notes + 4-language what's-new, docs/publishing/POST_LAUNCH.md (soft launch, KPIs, LiveOps calendar, triage rota, 1.0.1/1.1 roadmap), LAUNCH_CHECKLIST §8 1.0 gate; GDD band 5 paragraph, day-30 decision (pays once, no repeat), §8 Weekly Challenge proposal (WEEKLY-1).
- 2026-09-19 LV-8 (dad05b8) 19/20 lean tail fixed; no pool level below 47/50 on any twist at 50 seeds. DOCS-2 (aaa48b1) GDD consistency pass, §2.7 modifiers, §3 band table, PLAN status, TEAM ownership.
- 2026-09-19 MM-4 (ed9f8bf) DPR cap native 2 / web 2.5 (`?dprcap=N` override): iPhone-class layers 33.9 → 15.1 MB; device confirmation still pending (MM-1).
- 2026-09-19 QA review of the week (f0d7903, next commit): stale HUD layer on same-size resize fixed, hostile-save / i18n-key / rollover tests, lazy + layers e2e; BUG-8 lazy chunk recovery and BUG-9 daily restart after rollover fixed.
- 2026-09-19 LV-8 (no commit yet) lean-twist tail: 19 Toll Road `red` 14 → 12, 20 Roadblock `nw`/`ne` 8 → 10 (JSON only; lessons, names, personalities, clocks unchanged — plain medians 25.5 → 25.6 s and 23.6 → 22.7 s). Root cause in both: one enemy ate the other within 5 s (19: the rusher empties `red`, takes `top` then `green`; 20: the rusher empties `red` onto `nw` and the opportunist walks in through `red`–`green`) and the merged economy out-produced the lean player while its home sat at 0–2 units after the double expansion — never a mine/barrier problem. K=50: 19 lean 44 → 49, fastFeet 46 → 50 (plain/thinWalls/reinforced 50); 20 lean 43 → 50 (all others 50). Pool sweep after: no level < 47/50 on any twist (plain 1599/1600, lean 1585/1600, fastFeet 1600/1600, thinWalls 1599/1600, reinforced 1599/1600); `--seeds 20` 0 failed, `--upgrades max --seeds 10` median 2.0 ≤ 2.5, `--daily 2026-09-19 --days 60 --seeds 5` 60/60 days 300/300 runs, `npm run check` green (585 tests).
- 2026-09-18 PERF-3 (03f62eb) ground / game / HUD on three stacked canvases: CPU 4× level 40 rAF median 133 → 67 ms (−50 %), task time −37 %, pixel diff < 0.1 %; menu-only code moved to the lazy chunk (eager 79.5 kB).
- 2026-09-18 DAILY-2 (d14e358, 1304498) streak milestones day 3/7/30 → 5/20/100 crystals once per streak run, card shows the next bonus; CI gates: daily 30 days × 3 seeds and all five twists × 32 levels × 5 seeds (~15 s).
- 2026-09-18 M3-2 (ccfa9fe) four silhouette skins (Round keep 150, Watchtower 200, Shield bearers 120, Clockwork robots 150 crystals) in a lazy chunk with shop previews.
- 2026-09-18 BUG-7 (d3981e6) level 16 lean 30/50 → 50/50; QA-3 (1304498) `maxedModifiers()` in src/economy/maxUpgrades.ts pinned to the catalog.
- 2026-09-18 QA-3 (no commit yet) `maxedModifiers()` moved out of `scripts/playtest.ts` into `src/economy/maxUpgrades.ts` (with `maxedTiers()`, `maxedEffect(kind)`): the one catalog-derived "every Commander track at its cap" ladder for `--upgrades max` and any future tool (playtest.ts re-exports it; scratch `diag.mts` is not moved into the repo — it should import this helper instead of typing 1.2/1.25/+5/1.15). Regression test `tests/economy/maxUpgrades.test.ts` pins it to the catalog's max tiers, to `ADVANTAGE_LIMIT` (1.06 / 1.25 / +2 / 1.04) and to the game's `modifiersFromSave` at max tiers; the stale "at its cap" fixture comment in `tests/ai/modifiers.test.ts` now says it is the pre-retune fixture.
- 2026-09-18 DAILY-1 (01f3701, 1323695, 41c64c0, 6ea495d, b52491d, 288618f, c2353a1) Daily Challenge v1: deterministic level/seed/twist per UTC day (pool 9–40, five twists), level-map card with countdown/streak/DONE, rewards 30 + 10/star gold + 5 crystals once a day, save.challenge, e2e daily.spec, 17 i18n keys × 4; playtest `--daily` and `--twist` modes; GDD §7 + ECONOMY model; levels 10/13/15/27/31/34/38 retuned so every pool level is ≥ 49/50 on every twist; 60-day sweep 60/60 days, 300/300 runs. AI-4: capped keeps attack on burst alone + lateral supply (never fires in the campaign).
- 2026-09-18 M3-5 (measured, no code): tower sprite cache built and reverted (rAF median 116.6 → 100 ms = 14 %, task time 7 %); real cost is the full-screen terrain blit + HUD → PERF-3.
- 2026-09-18 AI-3 (3b66c42) reference player answers a hose from a drained source: `hoseReserve` (only landings from other roads count while the ribbon's source is drained and the tower bleeds), 3 tests; old level 37 seed 1 now won at 41 s; old level 9 seeds 2–3 are a level problem (no answer exists), fixed by 3f48a1d.
- 2026-09-18 LV-5 (1750354) level 2 lesson teaches "a tower under fire cannot recruit — answer every red stream" in EN/AZ/RU/TR; QA e2e `tutorial.spec.ts` plays tutorials 1–3 by touch on a fresh save and checks a cleared level shows none (checklist R1 done).
- 2026-09-18 PUB-8 (50041a4) store frame 06 re-rendered on the current level 9 map and now shows the under-fire badge (capture waits for a player tower under fire); QA closed BUG-3 residual (wallet guard), BUG-4/5/6 verified fixed, hygiene guard for scratch specs; M3-5 measured (script 9–15 % of frame at CPU 4×, no pooling needed).
- 2026-09-17 RULES-2.1 (3e4a388) sim+ai+render: "under fire" — a hostile landing pauses the target's production 1.5 s (`Tower.underFireUntilMs`, `isUnderFire`), 14 sim + 14 AI tests; bots exploit it (`siegeForce`, counter/parry rules, drained-supply unlink, two-hop threat); peach badge with crossed-swords pip + impact ring. L3 100 vs L3 100 now flips in 14 s; the all-L3 stalemates are gone.
- 2026-09-17 LV-4 (3f48a1d) levels: v2.1 balance (9, 12, 19, 26, 27, 28, 31, 33, 34, 37, 38, 39) + star clocks refit with the no-upgrade-flip boundary; 20 seeds 40/40, max-upgrades 20 seeds 0 failed gates, median 2★.
- 2026-09-17 I18N-2 (235def1) level names + lessons AZ/RU/TR on all 40 levels (validated, manifest-carried), skins/achievements/upgrade labels via locale keys, banner re-wrap.
- 2026-09-17 PUB-7 (577b894, 1b28bd0) store screenshots: 8-frame v0.4.0 set in EN/AZ/RU/TR with the in-game UI in each language, Apple 6.7/6.5 EN 9 frames; store texts/what's-new in 4 languages, README, privacy wording; version 0.4.0 build 4 (f8cf408).
- 2026-09-17 PERF-2 (2d8cb24) level JSON lazy-loaded per level via a generated manifest (eager 80.5 → 73.7 kB gzip; 77.6 kB after I18N-2), budget back to 80 kB.
- 2026-09-15 V2-1 (4bb3944) sim: rules v2 links and auto-upgrade — capacity ladder 25/50/100, `link`/`unlink` commands, `GameState.links`, `tryAutoUpgrade`, 120 ms round-robin `drain`, `pruneLinks` (sourceLost/roadCut/targetFull), `sendUnits`/`upgrade` kept as deprecated no-ops, snapshots include links; 36 new sim tests
- 2026-09-15 V2-2 (4bb3944) render: per-level tower sprites (footprint, height, second storey/keep) and owner-coloured stream ribbons with chevrons, draining rings, "L{n} needed for {n} streams" hint with shake
- 2026-09-15 V2-3 (4bb3944) ui/input: tap-to-link / tap-again-to-unlink, drag-to-link, HUD streams pill and per-tower stream chips, send-ratio toggle and setting removed, tutorial rewritten for streams (EN/AZ/RU/TR)
- 2026-09-15 V2-4 (4bb3944) ai: rusher/turtle/opportunist and the reference player on the link model (maintain → reinforce → capture → supply → attack → cutBridge) with the threat model (linkPending / linkRate / inflowRate / landings / holdReserve / fallsAtMs); GDD §2.0 implementation notes + §2.5 rewritten to match (docs, 2026-09-15)
- 2026-09-15 Day 3b: lazy screens (eager 74.8 kB gzip), i18n EN/AZ/RU/TR with settings picker + title chip, Nunito Cyrillic subset; reference player bridge-aware attack planning (level 40 worst 112 → 91 s, no regressions)
- 2026-09-15 Day 3: reference player + turtle cut bridges (13 + 8 tests); levels 6/21/23 → 100/100 (medians 43/38/60 s); version 0.3.0 build 3 synced to Android/iOS; README and release notes v0.3.0 final (tag `tower-clash-v0.3.0` exists locally only: the CCR git proxy accepts pushes to the working branch, not tags — create the release from the GitHub UI)
- 2026-09-14 Day 2b: true 20 s rewind continue with original star clock; terrain theme skins buy/equip (title + play re-lit); level 30 → 100/100; star times recomputed for 6/9/10/11/27/31/37; privacy policy v2.1 + site pages + listing aligned with no-ATT; release notes v0.3.0 status
- 2026-09-14 Day 2: achievements + booster crate + crystal→gold conversion; settings privacy options + support id; no ATT (non-personalised ads on iOS); dedicated skin sprites (7 roofs, 7 helmets) + terrain themes (dusk, winter night, neon); sim snapshot ring + continue API; levels 27/31/37 with marching enemy tanks (100/100 seeds, medians 53/51/71 s); Android WebView e2e project; privacy.html + support.html on the site; store shot 07 + IAP review frame; routine re-bound to the producer session
- 2026-09-13 Sprint 3 (monetization): market research + ECONOMY.md + catalog; gold/crystal wallet, save v3; shop (crystal packs, starter pack, remove ads, premium bundle, skins, 5 commander upgrade tracks); daily reward; rewarded/interstitial ads flow with caps; sim player modifiers; store/ads providers (RevenueCat, AdMob, fakes) + native config; modifier-aware AI + `--upgrades` playtest; privacy policy v2, IAP/ads store disclosures, accounts guide; bundle-size and secrets-aware CI
- 2026-09-13 Sprint 2b: threat-aware reference bot (levels 1–8 100/100 seeds, `--seeds` sweep, client-identical RNG), levels 10/11/20/35 retuned to 100/100, smoke test pinned to seed 1, Pages deploy non-fatal until enabled
- 2026-09-13 Sprint 2: levels 16–40 with all mechanics + star tuning (M2-1..5, M2-8, M1-10); boosters UI (M3-1); settings (M3-3); mute (M1-8b/ART-3); Capacitor plugins + version sync + release signing path (MM-2, PUB-5); GitHub Pages workflow + DEPLOY.md (MM-5/M4-1); content/perf e2e + replay tests; cheap shadows (ART-1), dust/biomes (ART-2), shake + button sfx (M2-7); App Store screenshots; release notes v0.2.0; reference-bot tank-factory loop fix
- 2026-09-13 M0-1 Scaffold `tower-clash/` (Vite + TS strict + Vitest + Playwright + ESLint)
- 2026-09-13 M0-2 Sim core with 60 unit tests (all GDD §2 mechanics incl. fortress, artillery, tanks, mines, barriers, bridges, boosters)
- 2026-09-13 M0-3 Canvas renderer, pointer input, game loop, HUD, win/lose overlay
- 2026-09-13 M0-4 Enemy AI v1 (rusher, turtle, opportunist), reference player, `npm run playtest`
- 2026-09-13 M0-5 Levels 1–5 + `npm run levels:check`
- 2026-09-13 M0-6 Playwright smoke test + screenshots
- 2026-09-13 M1-1 Fortress and artillery towers (sim + tests; no level uses them yet — see M1-4)
- 2026-09-13 M1-2 Title screen, level select grid with stars, result screen, localStorage save
- 2026-09-13 M1-3 Stars by time, coins on first clear (save logic; result-screen display pending M1-3b)
- 2026-09-13 M1-5 Send-ratio toggle (100 % / 50 %)
- 2026-09-13 M1-6 GitHub Actions CI: check + playtest + build + e2e
- 2026-09-13 M1-4 Levels 6–15 (fortress at 9, artillery at 12), all seeds green
- 2026-09-13 M1-7 Tutorial overlay levels 1–3
- 2026-09-13 M1-3b Coins on result screen, level locking
- 2026-09-13 M1-11 Pause menu + speed ×2
- 2026-09-13 M3-4 PWA manifest + service worker + icons (pulled forward)
- 2026-09-13 ART-v2 "Sunlit Clay Islands": trend research, art direction doc, lit/shade palette, long shadows, biomes, Fredoka type, claymorphic HUD/cards, winding level map, store shots regenerated
- 2026-09-13 M1-9/M2-6 Tower War-style visual overhaul: terrain, 3D towers, soldiers, particles, toy HUD (stakeholder request)
- 2026-09-13 M1-8 WebAudio sfx (M key mutes)
- 2026-09-13 PUB-1..4 Store listing EN/AZ, privacy policy, launch checklist, release notes v0.1.0, captioned store screenshots + feature graphic, public README
- 2026-09-13 MM-0 Capacitor Android + iOS projects, icons/splash, APK + iOS simulator CI, docs/MOBILE.md
